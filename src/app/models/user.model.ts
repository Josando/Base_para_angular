export class User {
   id: string;
   name: string;
   roles: string;
   // tslint:disable-next-line:variable-name
   auth_token: string;
   // tslint:disable-next-line:variable-name
   expires_in: number;
  }
